# 「又一局」Agent 开发规范

本规范适用于整个仓库。开发前阅读 [产品需求文档](docs/PRD.md)，以用户当前明确要求和 PRD 中的业务规则为依据；发现冲突时先说明并澄清，不擅自改变产品范围。

## 1. 产品目标与范围

- 项目名：`one-more-round`；产品名：又一局；标语：记下每一局的输赢与相聚。
- 产品是熟人小组的共同对局日记，手机浏览器优先，兼顾电脑；重点是快速记局、回忆与轻量统计。
- 当前版本为 V0.1 邀请试用版，中文界面、邮箱验证码登录、默认北京时间、组内内容私有。
- 已有游戏与玩家、不附照片的普通对局，目标是用户在 60 秒内完成记录；需要真实操作验证，不将目标表述为已有结果。
- 不增加实时计分、专属桌游结算规则、公开社区、评论聊天、陌生人组局、Elo、付费订阅、原生 App 或完整离线同步。
- 按 PRD 分阶段交付：先跑通账号、小组、玩家、手动游戏、记局与回顾；再补齐 BGG、照片、玩家关联和统计；最后进行真实小组试用。阶段完成不等于 V0.1 全部完成。

## 2. Agent 工作方式

1. 先检查 `git status`、相关目录规范、`go.mod`、前端清单和实际调用路径。保留用户已有修改，不擅自覆盖、回滚或格式化无关文件。
2. 编码前说明假设与可验证的成功标准。复杂任务给出简短步骤及各步验证方式；有歧义时列出解释，有更简单做法时直接指出。
3. 遇到业务规则冲突或影响数据语义的缺失信息，暂停相关实现并澄清；不把推测写成既定需求。可独立开展的工作继续推进。
4. 只修改当前任务相关内容，匹配现有风格。不顺手重构相邻代码，不为未来需求创建抽象、配置、插件或空目录。
5. 修复 Bug 先用针对性用例复现；新增校验覆盖异常输入；重构验证已有行为。文档或简单样式调整无需为了测试而测试。
6. 构建、检查和测试使用仓库实际存在的命令。缺依赖、缺授权、缺服务或未执行的验证必须如实说明，不能标记通过。
7. 交付说明写清改了什么、为什么、如何验证和剩余限制。未明确要求时不提交、推送或部署，不修改远端生产数据。

<!-- CODEGRAPH_START -->
## CodeGraph

In repositories indexed by CodeGraph (a `.codegraph/` directory exists at the repo root), reach for it BEFORE grep/find or reading files when you need to understand or locate code:

- **MCP tool** (when available): `codegraph_explore` answers most code questions in one call — the relevant symbols' verbatim source plus the call paths between them, including dynamic-dispatch hops grep can't follow. Name a file or symbol in the query to read its current line-numbered source. If it's listed but deferred, load it by name via tool search.
- **Shell** (always works): `codegraph explore "<symbol names or question>"` prints the same output.

If there is no `.codegraph/` directory, skip CodeGraph entirely — indexing is the user's decision.
<!-- CODEGRAPH_END -->

没有 CodeGraph 时优先使用 `rg` / `rg --files` 搜索。不要擅自创建索引。

## 3. 技术栈与待决策事项

- 后端：Go，模块为 `github.com/superwhys/one-more-round`，Go 版本以 `go.mod` 为准。
- 框架与基础设施工具：`github.com/miebyte/goutils`。执行 Go 开发时使用用户指定的 `goutils` 技能，按任务读取其 references，并核对锁定版本的实际源码/API。
- 使用 `cores`、`flags`、`ginutils` 等当前库模块；不混用旧库 `go-utils` 的 `superflags`、`ginlibs` 等名称。
- MySQL 持久化必须定义独立表 Model，并通过 `gorm.io/gen` 生成 Query，业务仓储实际使用生成的 Query。Model 修改后执行 `make generate`，生成文件随源码入库且不得手改；表结构由启动时的 GORM `AutoMigrate` 按 Model 维护。
- 前端：Vue 3 + TypeScript + Vite + pnpm。提交 `pnpm-lock.yaml`，不混入 npm/yarn 锁文件；确定版本后在项目中固定包管理器版本。
- 交付：单体 Go 服务，内嵌前端构建产物，前端和 API 同源；生产运行不依赖 Node、Vite 或外置前端目录。
- 数据库、迁移工具、照片存储、邮件服务商、会话实现及部署厂商尚未由 PRD 确定。到相关实现前提出最小可行方案，记录确认后的决策；不将示例目录或库能力当成已确定选型。
- 单二进制指服务与前端资源打包；业务数据、上传照片、运行配置和密钥需要独立持久化或注入，不嵌入可执行文件。

## 4. 后端结构与依赖方向

采用职责清晰的 DDD 分层：

```text
API → Application → Domain ← Infrastructure
main.go 负责依赖组装
```

目标目录约定如下，按实际功能逐步创建，不预生成空壳：

```text
main.go                    # 配置、依赖注入、服务启动
api/                       # HTTP 装配：api.go 路由表、common 会话与错误、middleware、router/<资源>.go
cmd/swagger/               # swag 生成入口与 docs 生成产物（随源码入库）
config/                    # 配置结构与启动校验
internal/app/dto/          # 用例输入输出 DTO，JSON tag 即对外契约
internal/app/ports/        # 事务、邮件、文件等技术端口
internal/app/services/     # Application Service：每个业务边界一个 XxxApp + AppContext
internal/converter/        # Model ↔ Domain ↔ DTO 的唯一转换入口
internal/domain/<上下文>/   # 聚合/实体、仓储接口、领域服务（identity、group、game、diary、photo）
internal/infra/            # 持久化与外部适配；infra/mysql 提供 RepositoryFactory
internal/errcode/          # 业务错误码 + HTTP 状态 + 提示文案
internal/pkg/secure/       # 随机标识与摘要生成
web/                       # Vue 项目与 Go 静态资源嵌入入口
web/src/                   # 前端源代码
web/dist/                  # Vite 构建产物，供 go:embed 使用
docs/                      # PRD、确认后的技术决策与接口说明
```

- 优先识别身份与试用资格、小组与玩家、游戏目录、对局记录的业务边界；照片属于对局访问边界，统计是对局数据的查询视图。不要按数据库表机械拆服务。
- Domain 承载业务不变量、状态变化和用例规则，按业务上下文建包；每个上下文声明自己的 `IXxxRepository` 与 `IService`，不依赖 Gin、GORM、数据库模型、Redis 或外部服务。
- API 仅处理绑定、身份提取、应用调用及协议错误映射：路由表集中在 `api/api.go`，资源路由在 `api/router`，会话与错误响应在 `api/common`，中间件在 `api/middleware`；不直接访问存储，不编排事务。
- Application 编排用例、权限判断及事务，通过接口使用基础设施；每个业务边界一个 `XxxApp`，依赖由 `services.AppContext` 注入，不依赖具体数据库连接、Gin Context 或 SMTP 客户端。
- Infrastructure 实现仓储与外部适配：`internal/infra/mysql` 的 `RepositoryFactory` 汇总各仓储，`WithTransaction` 派生绑定同一事务的实例，SQL 与 Model 转换不越过这层。
- 三类模型分离：持久化 Model、Domain 实体、应用 DTO。三者之间的转换统一放在 `internal/converter`，转换函数按 `<X>ModelToDomain`、`<X>DomainToModel`、`<X>DomainToDTO` 命名，不在业务代码里散落字段搬运。
- 接口由最窄的直接消费者定义：领域仓储接口属于对应 Domain，由 `ports.Repositories` 汇总为工作单元；邮件、图片文件等技术端口由使用它的 Application 定义。
- 错误统一用 `internal/errcode.Error`（业务码 + HTTP 状态 + 文案）：领域与用例返回预定义错误，API 通过 `common.HandleRouterError` 映射响应，不在各 Handler 重复 `switch`。
- `main.go` 是组合根。必选配置在 `Validate()` 中校验，必选依赖在构造或启动时校验一次；请求持续传递 `context.Context`，后台工作响应取消。
- 使用 goutils 生命周期能力，避免重复实现信号监听和优雅退出；仅引入当前功能实际需要的基础设施。

## 5. 必须保持的业务不变量

### 身份、小组与玩家

- 账号负责登录与权限，玩家档案负责历史参与；未注册昵称玩家不能访问小组。
- 首次注册需要有效试用邀请或小组邀请；试用邀请一次性授予注册资格，小组邀请允许验证邮箱后原子注册并加入指定小组。小组邀请 7 天内可多人使用、可撤销，加入前只展示小组名称，不展示记录。
- 所有组内读写、统计、缩略图和原图都在服务端验证当前成员身份。资源 ID 与 `group_id` 必须一致，不能仅凭前端传入的小组 ID 授权。
- 普通成员可查看组内记录、记局、添加游戏与玩家，只能修改或删除自己创建的记录；组主可管理组内记录、成员、邀请及玩家关联。
- 玩家按小组隔离，同名不自动合并；添加重名时提示已有玩家或要求可区分昵称。
- 账号申请关联玩家须经组主确认；每个账号在同一小组最多关联一个档案，档案最多关联一个账号，并在持久化层防止并发重复绑定。
- 退出或移除成员立即撤销其小组访问资格，保留历史参与记录。组主退出前必须完成身份转让。

### 游戏与外部资料

- 先搜本组游戏和中文别名；用户主动选择“搜索更多桌游”后才调用 BGG。
- 同组相同 BGG ID 复用已有条目；中文别名与外部原名分开保存。
- 自定义游戏可关联 BGG；若目标已存在，须组主确认合并并保留历史记录。
- BGG 只由后端调用，配置超时并缓存结果，令牌不能进入前端。已导入游戏与历史记录不依赖 BGG 实时可用。
- 开始接入时核实 BGG 官方最新授权、接口及来源标识要求。授权未获批时允许手动添加，不得声称外部检索已经完成。
- 外部超时、限流或授权失败有明确反馈，不阻断本地选择与手动添加；缺封面使用统一占位图。

### 对局与结果

- 每条对局只属于一个小组；游戏、玩家和已关联照片必须属于该小组。玩家至少一人且不重复。
- 个人竞技至少两人；可选择获胜玩家、共同获胜、全局平局或未记结果。共同获胜各记一胜，平局无人记胜。
- 组队竞技至少两队，各队至少一人，每名参与玩家恰好属于一队；结果与可选分数记录在队伍上，成员继承队伍结果，战绩与个人竞技分开。
- 合作记录全队胜利、全队失败或未记结果，可填团队分数；单人挑战使用合作模式，不混入竞技胜率。
- 不从分数推断结果。分数支持负数、小数，空值与零严格区分；前后端及存储使用一致的精度策略。
- 日期默认北京时间今天，支持历史日期；对局日期用日期语义传输，审计时间保存为明确时区的时间戳，展示时转换。
- 回忆最多 500 字，按 Unicode 字符而非字节校验；照片最多 3 张，每张原始上传文件不超过 2 MiB。
- PRD 未明确的组队结果枚举、数值精度及限制、邀请有效期等细节，在对应功能实现前确认并写入接口契约，不默默添加业务限制。

### 统计口径

- 游玩次数按对局记录计数，包含未记结果；小组总局数不能累加玩家参与次数。
- 玩过游戏数按对局关联的游戏去重；玩家参与次数按其参加的对局计数。
- 竞技胜率 = 胜局 / 已记录结果的竞技局；平局进入分母，未记结果排除。
- 合作成功率 = 成功局 / 已记录结果的合作局；未记结果排除。
- 按具体游戏和模式展示战绩，不生成跨游戏综合实力排名。比例同时显示样本数；分母为零显示“暂无数据”。
- 日期区间、游戏、玩家筛选对列表和统计使用一致语义，默认全部；修改、删除和游戏合并后结果一致。
- 统计从对局与参与结果计算，不另维护手填战绩；引入缓存前先确保失效策略可验证。

## 6. API、一致性与数据安全

- 业务接口统一使用 `/api/v1`。使用 cores 挂载时核对 `StripPrefix` 行为，避免重复前缀；健康检查、API 和前端静态路由各自明确。
- 请求和响应定义明确 DTO，前后端同步字段、可空值、枚举和错误码。列表分页并约束单次查询上限。
- 采用 goutils 的统一响应与错误映射，核实锁定版本行为；认证、权限、输入错误和冲突使用明确业务码与 HTTP 状态，不把 HTTP 200 等同成功。
- 创建对局使用服务端幂等机制：同一次提交重试复用幂等键，按账号和小组限定范围；并发重复请求只产生一条记录，同键不同内容返回冲突。按钮禁用不能代替幂等保证。
- 编辑和删除携带记录版本并原子校验；冲突提示刷新，不静默覆盖。保存最近修改人及修改时间。
- 对局、参与结果、队伍和照片关联需要一致提交；玩家关联、组主转让、游戏合并等多记录变更由应用层界定事务。
- 成员撤销与敏感写入的并发场景也必须校验有效权限，不能依赖长期缓存中的成员身份。
- 邮箱验证码具备过期、一次性消费、发送频率和尝试次数限制；不在响应或日志中泄露验证码。试用资格在服务端验证。
- 会话凭据不写入普通前端持久化状态；采用 Cookie 会话时配置 HttpOnly、生产 Secure、合适的 SameSite，并处理 CSRF。
- 表结构由 Model 经启动时 `AutoMigrate` 维护，Model 即 Schema 的唯一来源；业务表不使用外键，引用完整性由应用层校验。`AutoMigrate` 只增不删，删除列/表等破坏性变更必须先说明影响并单独提供版本化 SQL。约束与索引覆盖唯一关联、组内去重和常用查询。
- 不记录令牌、验证码、邀请密钥、感想正文或照片内容；日志保留定位问题所需的请求标识、错误及必要资源 ID。

## 7. 前端交互约定

- 使用 Vue 3 Composition API、`<script setup lang="ts">`，启用严格类型检查；避免 `any`，API 请求集中封装，页面不得重复拼装认证或错误处理。
- 按页面/业务职责组织组件，只有出现真实复用时再提取；不为首版引入无必要的状态管理、组件库或复杂抽象。
- 手机导航为「回顾」「桌游」「小组」，「记一局」始终容易找到；电脑沿用相同信息结构。
- 暖白背景、深色文字、少量橙色强调；首页以回忆时间线为主，结果同时用文字和图标表达，不只靠颜色。
- 表单默认当前小组和今天，优先显示最近游戏与玩家，玩家名单需用户确认。可选字段渐进展开，避免增加主流程输入。
- “再记一局”沿用游戏和玩家，清空结果、分数与回忆；不得复用上一局提交的幂等键或照片。
- 草稿按账号和小组隔离，在当前设备自动保存；保存失败保留输入，成功后清除，退出账号清除本地草稿。切换账号或小组不串用草稿。
- 删除需二次确认；加载、空数据、失败、重试、上传进度和并发冲突有可理解的中文反馈。
- 表单有可访问标签，支持键盘操作和清晰焦点；手机全流程无横向溢出，触摸目标易点击。

## 8. 图片处理

- 照片独立上传，失败可重试或移除；保存对局只关联成功上传的图片，失败图片不阻止其他内容保存。
- 后端校验上传大小、数量、实际解码格式和图片尺寸资源上限，不能只信扩展名或浏览器 MIME。
- 仅支持 JPEG、PNG、WebP；上传处理时压缩并去除定位元数据，服务端保证落盘与对外提供的图片满足要求。
- 使用服务端生成的存储标识，不接受客户端文件路径；未关联上传按上传人和小组授权，制定临时文件清理策略。
- 原图和缩略图通过验证成员权限的服务端入口访问，不以公开目录或永久公开 URL 暴露。缓存不得绕过退出/移除后的权限撤销。

## 9. 前端嵌入与构建

- Vite 产物统一输出到 `web/dist`，由 `web` Go 包中的 `go:embed` 嵌入，通过 `fs.Sub` 等标准能力提供服务。
- 构建顺序固定为：安装锁定前端依赖 → TypeScript 检查 → Vite 构建 → Go 检查/测试与二进制构建。缺少或过期的前端产物不能作为有效发布结果。
- 首次搭建时提供一个可重复运行的根目录构建入口，落实上述顺序并记录在 README；不要只留下依赖人工记忆的命令。
- 前端构建完成后再运行依赖嵌入资源的 Go 命令，避免 Vite 清理 `dist` 时与 Go 构建并发。
- 前端源码和锁文件入库，`dist`、`node_modules` 及本地二进制默认不入库；干净检出通过统一入口生成完整产物。测试若依赖 embed，同样先构建前端。
- SPA fallback 仅用于前端页面的 GET/HEAD 导航；不存在的 API、图片或静态资源应正确返回错误，不能返回 `index.html` 掩盖失败。
- 带内容 hash 的资源可长期缓存；HTML 入口应重新验证。开发通过 Vite 代理访问 API，生产同源，不默认开放任意来源 CORS。
- 构建验收必须将二进制放到不含 `web/dist` 的临时目录运行，验证首页、前端深链接刷新、静态资源、API 和缺失资源响应。所需配置/存储仍按已选方案提供。

## 10. 验证与完成标准

根据改动范围选择必要验证，相关功能至少覆盖以下行为：

| 范围 | 必须验证 |
| --- | --- |
| 领域规则 | 三种模式、共同获胜、平局、未记结果、负分/小数/空分数、重复玩家与非法分队 |
| 统计 | 不同模式隔离、样本为零、平局分母、筛选一致、编辑删除后更新 |
| 权限 | 非成员、已移除成员、跨组资源 ID、普通成员修改他人记录、原图/缩略图访问 |
| 一致性 | 重复提交与并发重试、版本冲突、玩家重复绑定、事务失败不留下半条记录 |
| 外部依赖 | BGG 超时/授权失败可降级、邮件失败反馈、上传失败不丢其余内容 |
| 前端闭环 | 一人记录整桌、草稿恢复和退出清理、再记一局、手机与电脑布局 |
| 发布产物 | 干净检出可构建、二进制独立提供前端、深链接与 API/资源错误正确分流 |

- Go 修改执行格式化和针对性测试；形成完整构建流程后执行 `go test ./...`、`go vet ./...` 及二进制构建，前提是嵌入资源已生成。
- 前端修改执行 TypeScript 检查、生产构建，以及涉及交互/规则的相关测试；页面行为用浏览器验证，不能只用编译通过代替。
- 差异检查使用 `git diff --check`；新增未跟踪文件也要单独审阅，因为普通 diff 不包含它们。
- “完成”需要真实流程可用且验证有证据；静态占位页、模拟接口、未授权的 BGG、未经实际操作的 60 秒目标都不得标记为完成。
- 技术监测聚焦保存失败率、图片上传失败率、外部搜索失败率与延迟；产品事件仅记录必要的开始、成功、耗时与回访，不包含用户正文或照片。
